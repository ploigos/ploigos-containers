name: tssc-tool-skopeo-pr
on:
  pull_request:
    paths:
      - tssc-tool-skopeo/**
jobs:
  build:
    env:
      context: tssc-tool-skopeo
      image_name: tssc-tool-skopeo
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Check and verify version.json
        id: check_version
        run: |
          echo -n ::set-output name=IMAGE_TAGS::
          echo $(jq -r '.version' ${context}/version.json)
      - name: Build image
        uses: docker/build-push-action@v1
        with:
          path: ${{ env.context }}
          push: false
          repository: ${{ env.image_name }}
          tags: ${{ steps.check_version.outputs.IMAGE_TAGS }}
      - name: Test image
        run: |
          echo "Running: docker run ${image_name}:${{ steps.check_version.outputs.IMAGE_TAGS }} --version"
          docker run ${image_name}:${{ steps.check_version.outputs.IMAGE_TAGS }} --version
